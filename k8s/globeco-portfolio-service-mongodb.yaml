apiVersion: v1
kind: Service
metadata:
  name: globeco-portfolio-service-mongodb
  namespace: globeco
  labels:
    app: globeco-portfolio-service-mongodb
spec:
  ports:
    - port: 27017
      targetPort: 27017
  clusterIP: None  # Headless service for StatefulSet
  selector:
    app: globeco-portfolio-service-mongodb
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: portfolio-service-mongodb-pvc
  namespace: globeco
  labels:
    app: globeco-portfolio-service-mongodb
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: local-storage
  selector:
    matchLabels:
      app: portfolio-service-mongodb
      type: local
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: globeco-portfolio-service-mongodb
  namespace: globeco
spec:
  serviceName: globeco-portfolio-service-mongodb
  replicas: 1
  selector:
    matchLabels:
      app: globeco-portfolio-service-mongodb
  template:
    metadata:
      labels:
        app: globeco-portfolio-service-mongodb
    spec:
      containers:
        - name: mongodb
          image: mongo:6.0
          ports:
            - containerPort: 27017
          volumeMounts:
            - name: mongodb-data
              mountPath: /data/db
          resources:
            requests:
              cpu: "300m"
              memory: "200Mi"
            limits:
              cpu: "500m"
              memory: "1Gi"
      volumes:
        - name: mongodb-data
          persistentVolumeClaim:
            claimName: portfolio-service-mongodb-pvc 